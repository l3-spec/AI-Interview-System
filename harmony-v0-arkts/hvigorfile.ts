// DevEco Studio generated build configuration.
// This file is used by the hvigor build system to build the app.
import { appTasks } from '@ohos/hvigor-arkui-compiler';

export default appTasks({
  tasks: ['jarCompile'],
  defaultParams: {
    compileMode: 'moduleMode',
    skipNestedConfig: true
  }
});
