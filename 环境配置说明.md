# 🌐 AI面试系统环境配置说明

## 📋 系统架构

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Android App   │    │ Admin Dashboard │    │  System Admin   │
│   (求职者端)     │    │   (企业管理端)   │    │  (系统管理端)    │
│   动态配置      │    │   Port: 5174    │    │   Port: 5175    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         └───────────────────────┼───────────────────────┘
                                 │
                    ┌─────────────────┐
                    │   Backend API   │
                    │   Port: 3001    │
                    └─────────────────┘
```

## 🔧 各端配置

### 1. Android App 配置

**文件位置**: `android-app/app/src/main/java/com/aiinterview/assistant/config/AppConfig.kt`

**快速切换环境**:
```kotlin
// 修改 CURRENT_ENV 即可切换环境
val CURRENT_ENV = Environment.DEVELOPMENT  // 开发环境
val CURRENT_ENV = Environment.STAGING      // 测试环境
val CURRENT_ENV = Environment.PRODUCTION   // 生产环境
```

**预设配置**:
- 开发环境: `http://192.168.101.26:3001/api/`
- 模拟器: `http://10.10.1.128:3001/api/`
- WiFi局域网: `http://192.168.1.100:3001/api/`
- 生产环境: `https://api.aiinterview.com/api/`

### 2. 企业管理端配置

**文件位置**: `admin-dashboard/src/config/constants.ts`

```typescript
export const AUTH_CONSTANTS = {
  TOKEN_KEY: 'admin_token',
  USER_KEY: 'admin_user',
  API_BASE_URL: 'http://localhost:3001/api'  // ✅ 已修复
};
```

### 3. 系统管理端配置

**文件位置**: `system-admin/src/config/config.ts`

```typescript
export const config = {
  API_BASE_URL: 'http://localhost:3001/api',  // ✅ 已修复
  API_TIMEOUT: 10000,
  UPLOAD_URL: 'http://localhost:3001/api/upload',
  // ...其他配置
};
```

### 4. 后端API配置

**文件位置**: `backend-api/src/config/index.ts`

```typescript
export const config = {
  port: parseInt(getEnvVar('PORT', '3001')),
  cors: {
    origin: [
      'http://localhost:5173',  // Vite默认端口
      'http://localhost:5174',  // 企业管理端
      'http://localhost:5175',  // 系统管理端
      'http://localhost:8080',  // 可能的其他端口
      'http://localhost:8081'
    ],
    credentials: true
  }
};
```

## 🚀 快速启动指南

### 方法1: 一键启动脚本
```bash
./start-all.sh
```

### 方法2: 分别启动
```bash
# 1. 启动后端API (端口3001)
cd backend-api && npm run dev

# 2. 启动企业管理端 (端口5174)
cd admin-dashboard && npm run dev

# 3. 启动系统管理端 (端口5175)
cd system-admin && npm run dev

# 4. 构建Android App
cd android-app && ./gradlew assembleDebug
```

## 🔍 问题排查

### 1. 404错误排查
- 检查API地址是否正确
- 确认后端服务是否启动
- 验证路由配置是否匹配

### 2. CORS错误排查
- 检查后端CORS配置
- 确认前端请求域名在允许列表中

### 3. 认证错误排查
- 检查JWT token是否正确
- 确认用户类型是否匹配

## 📱 Android App 调试技巧

### 1. 网络环境切换
```kotlin
// 在 AppConfig.kt 中快速切换
val BASE_URL = AppConfig.QuickSwitch.LOCAL_DEV      // 本地开发
val BASE_URL = AppConfig.QuickSwitch.EMULATOR_LOCAL // 模拟器
val BASE_URL = AppConfig.QuickSwitch.WIFI_LOCAL     // WiFi局域网
```

### 2. 查看环境信息
```kotlin
// 在应用中显示当前环境信息
Log.d("AppConfig", AppConfig.getCurrentEnvironmentInfo())
```

### 3. 网络安全配置
**文件位置**: `android-app/app/src/main/res/xml/network_security_config.xml`

```xml
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.101.26</domain>
        <domain includeSubdomains="true">10.10.1.128</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
```

## 🔐 测试账号

### 系统管理员
- 邮箱: `superadmin@aiinterview.com`
- 密码: `superadmin123`
- 访问: http://localhost:5175

### 企业用户
- 邮箱: `company@aiinterview.com`  
- 密码: `company123456`
- 访问: http://localhost:5174

### 求职者
- 邮箱: `user@aiinterview.com`
- 密码: `user123456`
- 访问: Android App

## 🛠️ 开发建议

1. **本地开发**: 使用 `192.168.101.26:3001` 
2. **模拟器调试**: 使用 `10.10.1.128:3001`
3. **WiFi调试**: 使用 `192.168.1.100:3001`
4. **生产部署**: 使用 `https://api.aiinterview.com`

## 📝 配置检查清单

- [ ] 后端API服务正常运行 (http://localhost:3001/api/health)
- [ ] 企业管理端配置正确 (http://localhost:5174)
- [ ] 系统管理端配置正确 (http://localhost:5175)
- [ ] Android App 网络配置正确
- [ ] CORS配置包含所有前端域名
- [ ] JWT认证配置正确
- [ ] 数据库连接正常 